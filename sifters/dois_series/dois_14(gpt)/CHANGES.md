# dois_14(gpt)

- New independently selectable reference, fixed-weather, shared-weather and creative
  presets. Default creative preserves the 4:3:2 proposal; reference matches dois_14.
- Normal MIDI files in mid/<preset>/, preset-labeled filenames, no symlinks or duplicate
  exports. One verified temporary render precedes per-file replacement.
- Retain the pure engine, strict settings, shared rarity option, explicit accent phase,
  complete note identity verification, full manifests and diagnostic CLI.
- --all processes all presets; --preset selects one. All selected plans are validated
  before rendering starts. --output-dir supplies the root for their subfolders.
- 43 tests retain musical coverage, add independent reference/preset-isolation checks,
  and replace tests for discarded machinery with ordinary-export checks.
- MUSICAL_DESIGN.md explains the factual correction, policy choices and creative
  reasoning separately. REVIEW_RESPONSE.md addresses Claude's feedback.

Previous versions are unchanged. No arrangement or Max work was added.
