parent_rhythm_velocity.json was extracted from the installed dois_14(gpt) MIDI
using the independent raw Standard MIDI File decoder from the earlier audit,
without mido or project functions. Each record is [onset ticks, duration ticks,
velocity]; pitch and channel are deliberately excluded because this version
changes them. The published pitch-offset set is separately pinned in test_pitch.py.
