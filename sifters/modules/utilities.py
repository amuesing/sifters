# import music21

# def save_midi(file_path, midi_file):
#     midi_file.write('midi', fp=file_path)
#     print(f'MIDI file saved at {file_path}')

# # Usage example:
# my_midi = music21.midi.MidiFile()
# save_midi('path/to/my_midi.mid', my_midi)


# # arg.dataframe.sort_values(by = 'Offset').drop_duplicates().to_csv(f'sifters/data/csv/.df_{arg.name}_{arg.id}.csv', index=False)
# # norm.sort_values(by = 'Offset').to_csv(f'sifters/data/csv/norm_{arg.name}_{arg.id}.csv', index=False)