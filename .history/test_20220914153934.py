import numpy as np

row = [1,2,3,4]

def generateSerialMatrix(row):
    interval = []
    columns = []
    matrix = []
    for tone in row:
        print(tone )
        # np.concatenate(columns,  x)
    # print(columns)
        
generateSerialMatrix(row)